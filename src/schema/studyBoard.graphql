type StudyBoard implements IStudyBoard {
  id: ID
  title: String!
  content: String!
  category: StudyBoardCategory!
  createdAt: String
  updatedAt: String
}

type GetStudyBoards implements IPagination {
  count: Int!
  rows: [StudyBoard]
}
