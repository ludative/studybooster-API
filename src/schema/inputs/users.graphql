# 사용자 Input (회원가입/회원수정 시 사용)
input UserInput {
  email: String!
  password: String!
  nickname: String!
  introduction: String
  profileImage: String
  isAdmin: Boolean
  deleted: Boolean
  isValidEmail: Boolean
}
